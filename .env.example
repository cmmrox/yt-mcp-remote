# Auth0 Configuration
# Your Auth0 tenant domain (e.g., "dev-abc123.us.auth0.com")
# IMPORTANT: Use the EXACT domain from your Auth0 Dashboard → Settings → Domain
# For US tenants created after June 2020, this will be *.us.auth0.com
# For EU tenants, this will be *.eu.auth0.com
# For AU tenants, this will be *.au.auth0.com
AUTH0_DOMAIN=dev-f3gxvglnmk4c6o1b.us.auth0.com

# Auth0 API identifier from your Auth0 dashboard
# This should match your API audience setting in Auth0
AUTH0_AUDIENCE=https://your-api-identifier.com # you can make this identical to RESOURCE_SERVER_URL

# Public URL where your MCP server is accessible
# Should end with "/mcp" path (e.g., "https://yourdomain.com/mcp")
RESOURCE_SERVER_URL=https://your-server-public-url.com/mcp

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG: Most verbose, shows all authentication steps (recommended for troubleshooting)
# INFO: Normal operation logs
# WARNING: Only warnings and errors
# ERROR: Only errors
LOG_LEVEL=DEBUG
