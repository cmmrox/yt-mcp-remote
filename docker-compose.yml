version: '3.8'

services:
  yt-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yt-mcp-server
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - PORT=8000
